import React from 'react';
import styles from './Header.scss'

const Header = () => {
    return (
        <div className={styles.header}>
            <span>Danny B's Time Logger</span>
        </div>
    )
};

export default Header;